# 类型层 @deepseek-ai/* 是 optional peer（import type 编译期使用），
# 运行期由 dsh 宿主注入；构建期由 scripts/build.sh junction-link 自 DSH core install，
# 不走 pnpm 自动安装——避免拉取未发布到公共 npm 的包（如 @deepseek-ai/dsh-type-meta）。
auto-install-peers=false
